# @volenday/column-address

> React Table column for address.

## Install

Using npm:

```sh
npm install --save @volenday/column-address
```

or using yarn:

```sh
yarn add @volenday/column-address
```
